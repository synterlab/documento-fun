{
  "name": "documento-fun",
  "version": "1.0.0",
  "description": "Documento.fun mobile app",
  "scripts": {
    "build": "echo Static site ready",
    "cap:sync": "npx cap sync",
    "cap:build": "bash build.sh"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  }
}
